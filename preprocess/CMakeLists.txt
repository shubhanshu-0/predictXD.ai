cmake_minimum_required(VERSION 3.10)

project(MyProject)

find_package(OpenCV REQUIRED)

include_directories(${OpenCV_INCLUDE_DIRS})

add_executable(MyProject preprocess.cpp)

target_link_libraries(MyProject ${OpenCV_LIBS})
